# Astro Runtime includes the following pre-installed providers packages: https://docs.astronomer.io/astro/runtime-image-architecture#provider-packages
apache-airflow-providers-weaviate==1.3.4
# Provider 1.3.x uses the Weaviate Python client v3 schema API.  Without an
# upper bound, pip selects client v4, which removed `weaviate.data`.
weaviate-client==3.25.3
langchain==0.1.16
